# Copy these into your Vercel project's Environment Variables.
# Do NOT put real values in this file — it is committed to git.

# The password you type to open the app. Choose something strong.
APP_PASSWORD=

# A long random string used to sign your login session (keep it secret).
# Generate one with:  openssl rand -base64 32
AUTH_SECRET=
